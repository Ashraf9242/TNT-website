import {defineMiddleware} from 'astro:middleware';
export const onRequest=defineMiddleware(async(_,next)=>{const response=await next();response.headers.set('X-Content-Type-Options','nosniff');response.headers.set('Referrer-Policy','strict-origin-when-cross-origin');response.headers.set('X-Frame-Options','DENY');response.headers.set('Permissions-Policy','camera=(), microphone=(), geolocation=()');if(import.meta.env.PUBLIC_CONTENT_APPROVED!=='true'||import.meta.env.PUBLIC_ALLOW_INDEXING!=='true'||!import.meta.env.PUBLIC_SITE_URL)response.headers.set('X-Robots-Tag','noindex, nofollow');return response;});

import {slugs,href,activeLanguages} from '../data/site';
export const GET=()=>{const base=import.meta.env.PUBLIC_SITE_URL?.replace(/\/$/,'');const approved=base&&import.meta.env.PUBLIC_CONTENT_APPROVED==='true'&&import.meta.env.PUBLIC_ALLOW_INDEXING==='true';const escape=(s:string)=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">${approved?slugs.flatMap(s=>activeLanguages.map(l=>`<url><loc>${escape(base+href(l as 'ar'|'en',s))}</loc><xhtml:link rel="alternate" hreflang="ar" href="${escape(base+href('ar',s))}"/>${activeLanguages.includes('en')?`<xhtml:link rel="alternate" hreflang="en" href="${escape(base+href('en',s))}"/>`:""}</url>`)).join(''):''}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}})};
